#!/usr/bin/env bash

script_path="lfi_scatter_plot_exp.py"

dataset_name="${1:-'TEP_ACME'}"

source "./dataset_config"

declare -A cfg
get_datapath "$dataset_name" cfg

dataset_path=${cfg[dataset_path]}
tep_name=${cfg[tep_name]}
coffe_name=${cfg[coffe_name]}
piade_prefix=${cfg[piade_prefix]}
smd_prefix=${cfg[smd_prefix]}
piade_suffix=${cfg[piade_suffix]}

model_name="$2"
interpretation="$3"
n_estimators=${4:-300}
scenario=${5:-2}
file_pos=${6:-0}

echo "#############################################"
echo "Producing LFI scatter plot for model $model_name and interpretation $interpretation"
echo "#############################################"

uv run $script_path \
  --dataset_name "$dataset_name" \
  --dataset_path "$dataset_path" \
  --model "$model_name" \
  --n_estimators "$n_estimators" \
  --interpretation "$interpretation" \
  --scenario $scenario \
  --file_pos $file_pos \
  --save_plot
